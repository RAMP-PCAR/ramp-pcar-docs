language: ruby

rvm:
  - 2.1

branches:
  only:
    - develop
    - master
    - /^test/.*$/
    - /^v\d+\.\d+\.\d+(-.+)?$/

before_install: 
  - npm install -g grunt-cli

script:
  - jekyll build

after_success: 
  - grunt release